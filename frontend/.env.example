# Leave empty for local dev — Vite proxies /api to http://localhost:5000
# (see vite.config.js). Set this to your deployed API's origin when the
# frontend and backend are hosted separately in production, e.g.:
# VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=
